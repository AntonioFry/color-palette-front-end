//is loading
//throw error
//pick project
//make new project
//pick palette
//lock color

export const setProjects = (projects) => ({
  type: 'SET_PROJECTS',
  projects
});

export const setPalettes = (palettes) => ({
  type: 'SET_PALETTES',
  palettes
});